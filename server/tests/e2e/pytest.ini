[pytest]
testpaths = .
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: critical path tests (run with -m smoke)
    slow: tests that take >10s
timeout = 120
timeout_method = signal
strict_markers = true
addopts = -v --tb=short
