import SwiftUI

enum CornerRadius {
    /// 4pt — tags, small buttons
    static let small: CGFloat = 4
    /// 8pt — cards, thumbnails
    static let medium: CGFloat = 8
    /// 12pt — dialogs, bottom sheets
    static let large: CGFloat = 12
}
